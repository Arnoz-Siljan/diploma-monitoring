input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  if [requestPath] == "/metrics" {
    drop { }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
  stdout { }
}
